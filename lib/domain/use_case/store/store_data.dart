import 'package:flutter_mask/domain/model/store.dart';

class StoreData extends Store {
  final double km;

  StoreData({required this.km});

  factory StoreData.fromStore(Store store) {
    return S
  }
}